# --- L9_META ---
# l9_schema: 1
# origin: l9-deployment-platform
# layer:
# - repository
# tags:
# - L9_META
# - deployment-platform
# owner: platform
# status: active
# --- /L9_META ---
- name: Install chrony
  ansible.builtin.apt: {name: chrony, state: present}
- name: Enable chrony
  ansible.builtin.service: {name: chrony, state: started, enabled: true}
