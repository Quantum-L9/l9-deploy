# --- L9_META ---
# l9_schema: 1
# origin: l9-deployment-platform
# layer:
# - repository
# tags:
# - L9_META
# - deployment-platform
# owner: platform
# status: active
# --- /L9_META ---
schema: l9.runtime-profile/v1
id: container-service
requires: [exact_image_digest, startup_probe, post_deploy_probe, non_root_or_image_defined_user]
allows: [public_ingress, persistent_volumes, external_dependencies]
forbids: [source_checkout, server_side_build, mutable_image_tag, docker_socket_mount]
