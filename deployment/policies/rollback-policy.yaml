# --- L9_META ---
# l9_schema: 1
# origin: l9-deployment-platform
# layer:
# - repository
# tags:
# - L9_META
# - deployment-platform
# owner: platform
# status: active
# --- /L9_META ---
schema: l9.rollback-policy/v1
rules:
  - previous release digest must be recorded and pullable
  - previous release must have a successful health receipt
  - automatic rollback may not execute a reverse migration unless explicitly contracted
  - rollback failure escalates to incident state and preserves evidence
