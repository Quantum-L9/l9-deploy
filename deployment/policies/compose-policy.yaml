# --- L9_META ---
# l9_schema: 1
# origin: l9-deployment-platform
# layer:
# - repository
# tags:
# - L9_META
# - deployment-platform
# owner: platform
# status: active
# --- /L9_META ---
schema: l9.compose-policy/v1
required: [no_new_privileges, restart_policy, external_runtime_network, env_file_mode_0600]
forbidden_mounts: [/var/run/docker.sock, /, /etc, /root]
forbidden_capabilities: [SYS_ADMIN, NET_ADMIN]
